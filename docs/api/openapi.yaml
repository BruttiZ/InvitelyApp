openapi: 3.1.0
info:
  title: Invitely API
  version: 0.1.0
  summary: API para convites digitais, RSVP e gestão de eventos.
  description: >
    Contrato público inicial da plataforma Invitely. As rotas públicas usam rate
    limiting e token de convite; as rotas administrativas exigem autenticação
    via Laravel Sanctum e políticas de tenant.
servers:
  - url: http://localhost:8082/api/v1
    description: Ambiente local Docker
tags:
  - name: Público
  - name: Auth
  - name: Admin
paths:
  /auth/login:
    post:
      tags: [Auth]
      summary: Emite token Sanctum para uso no dashboard Laravel.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                  example: usuario@empresa.com
                password:
                  type: string
                  format: password
                  example: senha-forte-123
                device_name:
                  type: string
                  example: local-dev
      responses:
        '200':
          description: Token emitido.
        '422':
          $ref: '#/components/responses/ValidationError'
  /auth/register:
    post:
      tags: [Auth]
      summary: Cria usuario e emite token Sanctum.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, email, password, role]
              properties:
                name:
                  type: string
                  example: Novo produtor
                email:
                  type: string
                  format: email
                  example: novo@invitely.dev
                password:
                  type: string
                  format: password
                  example: senha-forte-123
                role:
                  type: string
                  enum: [owner, guest, platform_admin]
                  example: owner
                device_name:
                  type: string
                  example: local-dev
      responses:
        '201':
          description: Usuario criado e token emitido.
        '422':
          $ref: '#/components/responses/ValidationError'
  /events/{slug}:
    get:
      tags: [Público]
      summary: Retorna a landing pública de um evento publicado.
      parameters:
        - $ref: '#/components/parameters/EventSlug'
      responses:
        '200':
          description: Evento encontrado.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    $ref: '#/components/schemas/Event'
        '404':
          $ref: '#/components/responses/NotFound'
  /events/{slug}/rsvp:
    post:
      tags: [Público]
      summary: Confirma ou recusa presença usando token de convite.
      parameters:
        - $ref: '#/components/parameters/EventSlug'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RsvpRequest'
      responses:
        '201':
          description: RSVP registrado.
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /admin/events:
    get:
      tags: [Admin]
      security:
        - sanctum: []
      summary: Lista eventos do tenant autenticado.
      responses:
        '200':
          description: Lista paginada de eventos.
    post:
      tags: [Admin]
      security:
        - sanctum: []
      summary: Cria um evento para o tenant autenticado.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventCreateRequest'
      responses:
        '201':
          description: Evento criado.
        '422':
          $ref: '#/components/responses/ValidationError'
  /admin/events/{event}/analytics:
    get:
      tags: [Admin]
      security:
        - sanctum: []
      summary: Retorna métricas operacionais de RSVP e check-in.
      parameters:
        - name: event
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Métricas do evento.
  /admin/events/{event}/check-ins:
    post:
      tags: [Admin]
      security:
        - sanctum: []
      summary: Registra check-in de um convidado usando o token do QR Code.
      parameters:
        - name: event
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [invite_token]
              properties:
                invite_token:
                  type: string
                  example: demo-invite-token
                method:
                  type: string
                  enum: [qr_code, manual]
                  default: qr_code
      responses:
        '201':
          description: Check-in registrado.
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/ValidationError'
  /admin/events/{event}/guests/export:
    get:
      tags: [Admin]
      security:
        - sanctum: []
      summary: Exporta convidados de um evento em CSV.
      parameters:
        - name: event
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Arquivo CSV de convidados.
          content:
            text/csv:
              schema:
                type: string
components:
  securitySchemes:
    sanctum:
      type: http
      scheme: bearer
  parameters:
    EventSlug:
      name: slug
      in: path
      required: true
      schema:
        type: string
        example: invitely-launch-night
  responses:
    NotFound:
      description: Recurso não encontrado.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    ValidationError:
      description: Erro de validação.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ValidationError'
  schemas:
    Event:
      type: object
      required: [id, name, slug, status, starts_at, timezone, hero, content, theme, gallery]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        slug:
          type: string
        status:
          type: string
          enum: [draft, published, archived]
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: [string, 'null']
          format: date-time
        timezone:
          type: string
        venue:
          type: object
          properties:
            name:
              type: [string, 'null']
            address:
              type: [string, 'null']
        spotify_playlist_url:
          type: [string, 'null']
          format: uri
        hero:
          type: object
        content:
          type: object
        theme:
          type: object
        gallery:
          type: array
          items:
            type: object
    RsvpRequest:
      type: object
      required: [invite_token, status, companions]
      properties:
        invite_token:
          type: string
          example: demo-invite-token
        status:
          type: string
          enum: [accepted, declined]
        companions:
          type: integer
          minimum: 0
          maximum: 20
        message:
          type: [string, 'null']
          maxLength: 1000
        answers:
          type: [object, 'null']
    EventCreateRequest:
      type: object
      required: [name, starts_at, timezone, hero, content, theme]
      properties:
        name:
          type: string
          maxLength: 160
        slug:
          type: string
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: [string, 'null']
          format: date-time
        timezone:
          type: string
          example: America/Sao_Paulo
        hero:
          type: object
        content:
          type: object
        theme:
          type: object
    Error:
      type: object
      required: [message]
      properties:
        message:
          type: string
    ValidationError:
      type: object
      required: [message, errors]
      properties:
        message:
          type: string
        errors:
          type: object
